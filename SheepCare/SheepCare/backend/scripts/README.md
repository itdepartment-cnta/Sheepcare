# backend/scripts

Standalone utility scripts for manually invoking project models or pipelines outside the app (not imported by the backend). Excel files passed in or generated here are gitignored (`backend/scripts/*.xlsx`).
